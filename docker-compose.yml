version: '3.8'

services:
  web:            # ✅ indent 2 spaces ใต้ services
    build:
      context: .
      dockerfile: apps/edu-flow-front/Dockerfile
    container_name: edu-flow-web
    restart: unless-stopped
    ports:
      - "3000:3000"