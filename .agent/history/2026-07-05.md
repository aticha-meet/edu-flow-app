# 📝 Session Log — 5 กรกฎาคม 2026

เอกสารนี้บันทึกสิ่งที่ทำในช่วง session วันที่ 5 กรกฎาคม 2026 ครอบคลุมการแก้ไข Middleware, Next-Auth, และการออกแบบหน้า Class

---

## 1. 🛡️ เขียน Middleware ใหม่ทั้งหมด (`src/middleware.ts`)

**ปัญหา:** ไฟล์ middleware เดิมเป็น code เก่าจากโปรเจค Adapter CMS ทั้งหมด ไม่ตรงกับโปรเจค EduFlow เลย มี syntax error, dead code, และ reference ถึง path `/webcms2024` ที่ไม่มีในโปรเจคนี้

**สิ่งที่ทำ:**
- ลบ code เก่าทั้งหมด เขียนใหม่ให้สะอาด
- Logic หลัก: ตรวจ cookie `ac_tk` ถ้าไม่มีให้ redirect ไป `/login`
- กำหนด `PUBLIC_PATHS = ['/login', '/callback', '/api/auth']`
- ใช้ `matcher` regex ครอบคลุมทุก path ยกเว้น static assets
- เพิ่ม `?redirect=<path>` query เพื่อให้ redirect กลับหลัง login

```ts
// Flow
ไม่ใช่ public path → ไม่มี ac_tk cookie → redirect /login?redirect=<path>
```

---

## 2. 🔧 แก้ `CLIENT_FETCH_ERROR` จาก Next-Auth

**ปัญหา:** `[next-auth][error][CLIENT_FETCH_ERROR] "Unexpected token '<', "<!DOCTYPE "... is not valid JSON"`

**สาเหตุ:** Middleware ไป block `/api/auth/session` ของ next-auth เพราะไม่มี `ac_tk` cookie → redirect เป็น HTML ไปให้ next-auth แทน JSON ทำให้ parse ไม่ได้

**วิธีแก้:** เพิ่ม `/api/auth` เข้า `PUBLIC_PATHS` เพื่อให้ next-auth internal routes ผ่านได้เสมอ

---

## 3. 🔑 ดึง `refreshToken` จาก Next-Auth JWT ใน Middleware

**เป้าหมาย:** เข้าถึง `refreshToken` ที่เก็บไว้ใน JWT cookie ของ next-auth โดยตรงจาก middleware โดยไม่ต้องเรียก API

**วิธีการ:**
```ts
import { getToken } from 'next-auth/jwt';

const jwtToken = await getToken({
    req: request,
    secret: process.env.NEXTAUTH_SECRET, // ต้องใช้ NEXTAUTH_SECRET เท่านั้น
});
const refreshToken = jwtToken?.refreshToken as string | undefined;
```

**Flow ที่ next-auth เก็บ refreshToken:**
```
Google OAuth → account.refresh_token
             ↓ jwt() callback ใน OauthConfig.ts (บรรทัด 35)
       token.refreshToken = account.refresh_token
             ↓ encrypt ลงใน JWT cookie
       cookie: "next-auth.session-token"
             ↓ getToken() ใน middleware decode ออกมา
       jwtToken.refreshToken ✅
```

**หมายเหตุเพิ่มเติม:** ต้อง extend `JWT` interface ใน `src/types/next-auth.d.ts` ด้วย:
```ts
declare module "next-auth/jwt" {
    interface JWT {
        refreshToken?: string;
        expiresAt?: number;
    }
}
```
เพื่อให้ TypeScript รู้จัก field `refreshToken` ใน JWT payload

---

## 4. 🐛 แก้ Bug `TypeError: Cannot read properties of null (reading 'refreshToken')`

**สาเหตุ:** 2 ปัญหาในบรรทัดเดียวกัน
1. ใช้ `secret: process.env.NEXT_PUBLIC_API_JWT_SECRET` ผิด env var → `getToken()` ถอดรหัสไม่ได้ → return `null`
2. Cast เป็น `{ refreshToken: string }` ทั้งที่ยังไม่ตรวจว่าเป็น null → `null.refreshToken` → crash

**วิธีแก้:**
```ts
// ❌ ก่อน
const session = await getToken({ secret: process.env.NEXT_PUBLIC_API_JWT_SECRET }) as { refreshToken: string }
console.log(session.refreshToken) // crash ถ้า null

// ✅ หลัง
const session = await getToken({ secret: process.env.NEXTAUTH_SECRET });
const refreshToken = session?.refreshToken as string | undefined; // null-safe
```

---

## 5. 🎨 ออกแบบหน้า Class (`src/app/class/`)

**สร้างไฟล์ใหม่:**
- [`page.tsx`](file:///F:/project-working/edu-flow/apps/edu-flow-front/src/app/class/page.tsx)
- [`class.module.scss`](file:///F:/project-working/edu-flow/apps/edu-flow-front/src/app/class/class.module.scss)

**Features:**
| Feature | รายละเอียด |
|---|---|
| **Skeleton Loading** | shimmer animation 6 cards, `setIsLoading(false)` อยู่ใน `finally` |
| **Mock Data** | 6 รายวิชา แต่ละวิชามี color, status, instructor, schedule, room, progress |
| **Stats Row** | จำนวนรายวิชา / กำลังเรียน / นักศึกษารวม |
| **Filter Tabs** | กรองตาม `all`, `active`, `upcoming`, `completed` |
| **Class Cards** | color accent bar, progress bar นักศึกษา, badge status |
| **Empty State** | แสดงเมื่อไม่พบรายวิชาในหมวดที่กรอง |
| **Responsive** | grid collapse เป็น single column บนมือถือ |

**Loading pattern ที่ใช้:**
```ts
const fetchClasses = async () => {
    setIsLoading(true);
    try {
        await new Promise((res) => setTimeout(res, 1200)); // mock delay
        setClasses(MOCK_CLASSES);
    } catch (err) {
        console.error(err);
    } finally {
        setIsLoading(false); // ← วางใน finally เสมอ
    }
};
```

---

## 📋 สิ่งที่ต้องทำต่อ (Next Steps)

1. ~~**Middleware Logic เพิ่มเติม**~~ ✅ เสร็จแล้ว
2. ~~**หน้า Callback — refresh token flow**~~ ✅ เสร็จแล้ว
3. **หน้า Class**
   - เปลี่ยน mock data เป็น API call จริง ผ่าน `apiClient.ts`
   - เพิ่มการ navigate ไปยัง class detail page เมื่อกด "ดูรายละเอียด"
4. **Next-Auth Type Extension**
   - เพิ่ม `refreshToken` และ `expiresAt` ใน `JWT` interface ใน [`next-auth.d.ts`](file:///F:/project-working/edu-flow/apps/edu-flow-front/src/types/next-auth.d.ts)
5. **Backend — สร้าง `POST /auth/refresh`**
   - รับ `refreshToken` จาก `Authorization: Bearer <token>`
   - verify กับ Google → ออก `ac_tk` cookie ใหม่

---

## 6. ⚙️ Class Controller (`pkg/class/controller.ts`) — ดึง Class ตาม userId

**ปัญหาเดิม:** controller เปล่า มีแค่ `return res.status(200).json({ message: "OK" })`

**สิ่งที่ทำ:**
- decode `ac_tk` cookie ด้วย `jwt.verify()` → ได้ `{ id, role }`
- แยก query ตาม role:

| Role | Prisma Query |
|---|---|
| `TEACHER` | `findMany({ where: { teacherId: userId } })` |
| `STUDENT` | `findMany({ where: { enrollments: { some: { studentId: userId } } } })` |

- include `teacher` และ `enrollments` เพื่อให้รู้จำนวนนักเรียนในแต่ละ class
- เพิ่ม `handleRefreshToken` middleware ใน [`router.ts`](file:///F:/project-working/edu-flow/apps/edu-flow-api/src/router.ts) ให้กับ `ClassRouter` (เดิมไม่มี)

```ts
// router.ts — ก่อน
AppRouter.use(ClassRouter); // ❌ ไม่มี middleware

// หลัง
AppRouter.use(handleRefreshToken, ClassRouter); // ✅
```

---

## 7. 🔄 Token Refresh Flow — Middleware + Callback

### Middleware (`src/middleware.ts`)

**ปัญหาเดิม:** ใช้ `getSession()` จาก `next-auth/react` ซึ่งเป็น client-side only → ใช้ใน middleware ไม่ได้

**วิธีแก้:** เปลี่ยนเป็น `getToken()` จาก `next-auth/jwt` ซึ่ง server-side และอ่าน JWT cookie ได้โดยตรง

**Logic ใหม่:**
```
ไม่มี ac_tk cookie
    ↓
มี refreshToken ใน JWT session?
    → ใช่ → redirect /callback?redirect=<path>   (silent refresh)
    → ไม่ใช่ → redirect /login?redirect=<path>   (force login)
```

### Callback Page (`src/app/callback/page.tsx`)

**เพิ่ม 2 modes:**

| Mode | สถานการณ์ | การทำงาน |
|---|---|---|
| **Fresh login** | เพิ่งล็อกอินจาก OAuth | ดึง session → ส่ง refreshToken ไป backend |
| **Silent refresh** | middleware redirect มา (`?redirect=`) | ดึง session → ส่ง refreshToken → redirect กลับหน้าเดิม |

**Step ใน callback:**
```
Step 1: fetch /api/auth/session  → ได้ { refreshToken }
Step 2: POST /auth/refresh       → backend set ac_tk cookie ใหม่
Step 3: router.push(redirectPath) → กลับหน้าที่ user ต้องการ
```

**Error cases:**
- OAuth error → แสดง error message
- ไม่มี session → แสดง "ไม่พบข้อมูลเซสชัน"
- ไม่มี refreshToken → แสดง "โปรดเข้าสู่ระบบใหม่"
- backend `/auth/refresh` fail → แสดง "ไม่สามารถต่ออายุ Token"

---

## 📋 สิ่งที่ต้องทำต่อ (อัปเดต)

1. **Backend — สร้าง `POST /auth/refresh`** ← สำคัญที่สุด
   - รับ `Authorization: Bearer <refreshToken>`
   - เรียก Google API เพื่อ verify และ exchange token ใหม่
   - set `ac_tk` cookie กลับให้ front
   - อยู่ใน `pkg/auth/` (ยังไม่มี pkg นี้)

2. **หน้า Class — เชื่อม API จริง**
   - เปลี่ยน mock data → `GET /class` ผ่าน `apiClient.ts`

3. **Next-Auth Type Extension**
   - เพิ่ม `refreshToken?: string` และ `expiresAt?: number` ใน `JWT` interface ของ [`next-auth.d.ts`](file:///F:/project-working/edu-flow/apps/edu-flow-front/src/types/next-auth.d.ts)

4. **Class Detail Page**
   - สร้าง `/class/[id]` route
   - ดึงรายละเอียด class, รายชื่อนักเรียน, assignment
